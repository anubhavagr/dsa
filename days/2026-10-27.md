# Day 32 — Tue Oct 27 · Trees II — Depth, Diameter & Balance
Week 5 · Phase 2: Core Data Structures
[← Prev](2026-10-26.md) · [Dashboard](../README.md) · [Next →](2026-10-28.md)

## 🎯 Concepts (~45 min)
- [ ] Read: [cheatsheets/11-trees § post-order tree DP](../cheatsheets/11-trees.md) — children answer first, the parent combines
- [ ] Read: [cheatsheets/11-trees § legs vs bends](../cheatsheets/11-trees.md) — a straight leg can go up to the parent; a bend is recorded locally
- [ ] Read: [cheatsheets/11-trees § early-exit sentinels](../cheatsheets/11-trees.md) — returning -1 to poison every ancestor's computation
- [ ] Read: [cheatsheets/11-trees § nodes vs edges](../cheatsheets/11-trees.md) — the off-by-one that ruins diameter answers
- [ ] Watch: [NeetCode practice — Trees](https://neetcode.io/practice) (pick matching video)

## 🔥 Pattern of the Day: Post-Order Tree DP — Return (depth, best)
A node can hand its parent only a *straight leg* — one downward path that continues upward — but the *best full answer* may bend at the node itself, joining both children's legs.
So every subtree returns two numbers: `depth` (the leg the parent is allowed to reuse) and `best` (a finished answer that never extends further). Maximum depth, diameter, and balance are all this template with a different combine step.

- **Reach for it when:** the answer at a node is a function of its children's answers.
- **Key invariant:** `depth` strictly feeds the parent; `best` is already complete.
- **Watch out:** count edges or nodes — pick one and say it out loud.
- **Interview soundbite:** "I return a leg and a best — the leg feeds my parent, the bend stays home."
- **Test out loud:** single node · a one-sided chain (diameter = height) · empty tree.
- **Cost:** O(n) time — one visit per node, no memo ever; O(h) recursion space.

```python
def postorder_dp(root):
    """Every node returns (depth, best) to its parent."""
    if root is None:
        return 0, 0                       # depth feeds the parent; best is done
    ld, lb = postorder_dp(root.left)
    rd, rb = postorder_dp(root.right)
    depth = 1 + max(ld, rd)               # the straight leg extends upward
    best = max(lb, rb, ld + rd)           # the answer that BENDS at this node
    return depth, best

def diameterOfBinaryTree(root):           # LC 543: longest path, in edges
    return postorder_dp(root)[1]

def isBalanced(root):                     # LC 110: sentinel = early exit
    def depth(n):
        if n is None:
            return 0
        L, R = depth(n.left), depth(n.right)
        if L < 0 or R < 0 or abs(L - R) > 1:
            return -1                     # -1 poisons every ancestor
        return 1 + max(L, R)
    return depth(root) >= 0
```

## ✅ Problems
- [ ] 1. [Maximum Depth of Binary Tree](https://leetcode.com/problems/maximum-depth-of-binary-tree/) (Easy)
      - Pattern: post-order depth rollup
      - Hint: If both children tell you their depths, what one expression is yours — and who supplies the +1?
      - Narrate: "depth of null is 0; every real node adds one on the way back up."
      - Time: ___ min · struggled? → mark below
      - [ ] 🔁 flag for Saturday redo

- [ ] 2. [Diameter of Binary Tree](https://leetcode.com/problems/diameter-of-binary-tree/) (Easy)
      - Pattern: (depth, best) tuple
      - Hint: A path through this node bends here — which two numbers from the children does the bend use, and what single number still goes up?
      - Narrate: "left leg + right leg is a candidate; only the longer single leg continues to my parent."
      - Time: ___ min · struggled? → mark below
      - [ ] 🔁 flag for Saturday redo

- [ ] 3. [Balanced Binary Tree](https://leetcode.com/problems/balanced-binary-tree/) (Easy)
      - Pattern: sentinel early-exit
      - Hint: Checking heights top-down re-walks subtrees — what single return value can mean both "my height" and "already broken below me"?
      - Narrate: "each call is O(1) work after its children — one pass, no recomputation."
      - Time: ___ min · struggled? → mark below
      - [ ] 🔁 flag for Saturday redo

## 🧠 What I learned
- Could I write the (depth, best) template cold, or did I mix the two numbers up?
- Where did the nodes-vs-edges off-by-one try to sneak in?
- Why does no tree-DP solution ever need memoization? (Each node visited once.)
- Did the -1 sentinel feel natural, or would an exception/tuple flag read better aloud?
- Which problem would fail first if the tree were a million nodes deep — and why?
- One sentence for the solutions/ header — the bug or insight I hit today:

## 🔁 Redo queue
- [ ] 🔁 (auto: anything flagged above)

> 🗣️ **Interview reps:** clarify constraints → example → approach aloud → complexity → code → test edge cases.
