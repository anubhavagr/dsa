# Day 41 — Thu Nov 05 · Tries
Week 6 · Phase 2: Core Data Structures
[← Prev](2026-11-04.md) · [Dashboard](../README.md) · [Next →](2026-11-06.md)

## 🎯 Concepts (~45 min)
- [ ] Read: [cheatsheets/14-tries § node as dict](../cheatsheets/14-tries.md) — a node IS its children map, plus the end flag
- [ ] Read: [cheatsheets/14-tries § insert and search](../cheatsheets/14-tries.md) — walk, create on demand, mark termination
- [ ] Read: [cheatsheets/14-tries § wildcard DFS](../cheatsheets/14-tries.md) — '.' forks over every child instead of stepping
- [ ] Read: [cheatsheets/14-tries § trie vs hashmap](../cheatsheets/14-tries.md) — when prefix sharing pays for itself
- [ ] Watch: [NeetCode practice — Tries](https://neetcode.io/practice) (pick matching video)

## 🔥 Pattern of the Day: Prefix Tree with Dict Nodes
A trie shares prefixes: inserting costs O(word length), and "is this prefix live?" is a walk, not a scan.
Make the node a dict subclass so `node[ch]` is the child edge, and carry one boolean — `end` — which is what distinguishes a stored word from a prefix that merely passes through. Wildcard search turns one deterministic walk into a DFS that forks at every '.'.

- **Reach for it when:** many strings + prefix questions, or board search with a shared dictionary.
- **Key invariant:** `end == True` marks "a real word terminates here", not "children exist below".
- **Watch out:** search vs startsWith differ only in the final-node check — say which you're doing.
- **Interview soundbite:** "The node is the map; the flag marks a word."
- **Test out loud:** empty word · a query that equals a stored prefix · an all-dots query.
- **Cost:** insert/search O(L); each dot forks O(26); board search is exponential in the worst case, pruned hard.

```python
class TrieNode(dict):                     # a node IS its children map
    def __init__(self):
        self.end = False                  # a stored word terminates here

class Trie:
    def __init__(self):
        self.root = TrieNode()

    def insert(self, word):
        n = self.root
        for ch in word:
            n = n.setdefault(ch, TrieNode())   # descend, creating as needed
        n.end = True

    def search(self, word):               # '.' = wildcard in this variant
        def dfs(i, node):
            if i == len(word):
                return node.end           # must be a WORD, not a prefix
            ch = word[i]
            if ch == '.':                 # fork over EVERY child
                return any(dfs(i + 1, c) for c in node.values())
            return ch in node and dfs(i + 1, node[ch])
        return dfs(0, self.root)
```

## ✅ Problems
- [ ] 1. [Implement Trie (Prefix Tree)](https://leetcode.com/problems/implement-trie-prefix-tree/) (Medium)
      - Pattern: dict-node trie, insert/search/startsWith
      - Hint: What is the difference between `search` and `startsWith` at the final node — and what does the node store to tell them apart?
      - Narrate: "walk the word; at the end, search demands the end flag, startsWith doesn't."
      - Time: ___ min · struggled? → mark below
      - [ ] 🔁 flag for Saturday redo

- [ ] 2. [Design Add and Search Words Data Structure](https://leetcode.com/problems/design-add-and-search-words-data-structure/) (Medium)
      - Pattern: wildcard DFS at '.'
      - Hint: A normal character step does what at a node — and what must '.' do instead, at what branching cost?
      - Narrate: "literal char: one child; dot: try all children — worst case explodes like a DFS."
      - Time: ___ min · struggled? → mark below
      - [ ] 🔁 flag for Saturday redo

- [ ] 3. [Word Search II](https://leetcode.com/problems/word-search-ii/) (Hard)
      - Pattern: trie + board DFS with pruning
      - Hint: Running full DFS per word re-walks shared prefixes — what structure merges them, and what lets you abandon a whole subtree mid-search?
      - Narrate: "one trie for all words; DFS marks cells visited; dead branches prune themselves when the node has no children."
      - Time: ___ min · struggled? → mark below
      - [ ] 🔁 flag for Saturday redo

## 🧠 What I learned
- Could I sketch the TrieNode (dict + end flag) from memory in 30 seconds?
- Where did prefix-vs-word bite — which check did I forget?
- In word search II — did the pruning (empty-children removal) make sense, or feel like magic?
- What complexity did I claim for wildcard search, and did the worst case justify it?
- What would I change if the alphabet were fixed at 26 — array of children?
- One sentence for the solutions/ header — the bug or insight I hit today:

## 🔁 Redo queue
- [ ] 🔁 (auto: anything flagged above)

> 🗣️ **Interview reps:** clarify constraints → example → approach aloud → complexity → code → test edge cases.
